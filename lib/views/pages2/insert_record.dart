import 'package:flutter/material.dart';

class InsertRecord extends StatelessWidget {
  const InsertRecord({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("Insert Recored"),
      ),
    );
  }
}
